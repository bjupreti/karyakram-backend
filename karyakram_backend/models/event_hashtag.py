# from sqlmodel import SQLModel, Field
# from datetime import datetime

# class EventHashtag(SQLModel, table=True):
#     id : int = Field(default=None, primary_key=True,index=True)  
#     hashtag_id: int
#     event_id: int
